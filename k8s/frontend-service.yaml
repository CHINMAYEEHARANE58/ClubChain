apiVersion: v1
kind: Service
metadata:
  name: clubchain-frontend
  namespace: clubchain
spec:
  type: ClusterIP
  selector:
    app: clubchain-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
